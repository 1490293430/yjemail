# 复制此文件为 .env 并修改配置

# JWT 密钥（必须修改）
JWT_SECRET_KEY=your_secret_key_here

# 数据加密密钥（可选）
# 用于加密数据库中的敏感信息（邮箱密码、refresh_token、client_id）
# 如果不设置，将从 JWT_SECRET_KEY 派生
# 建议设置一个独立的 32 字节 base64 编码密钥
# 可以使用以下命令生成: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Graph API Webhook URL（可选）
# 如果你有公网 HTTPS 地址，可以启用 Webhook 实时推送
# 微软会在收到新邮件时主动通知你的服务器
# 格式: https://你的域名/api/graph/webhook
# 示例: https://mail.example.com/api/graph/webhook
GRAPH_WEBHOOK_URL=
